

<%= link_to "メニュー追加",new_menu_list_path(@menu_list) %>

<%= link_to "登録メニューの一覧",menu_list_path(current_user) %>

<%= link_to "家族名を追加する",new_family_path(current_user) %>

<%= link_to "ログアウト", destroy_user_session_path, method: :delete %>

<p>現在登録されているメニュー数は<%= @menu_list.count%>個です</p>

<!--表示されるメニューをセットする -->
<% if @menu_list.present?%>
  <% randam_menu = @menu_list.offset(rand(@menu_list.count)).first %>
  <%= randam_menu.menu_name %>
  <%= image_tag randam_menu.image.url, :size =>'300x300' %>
<% end %>

<button type="button" id="menu_button"><%= link_to "メニューを決める",menu_lists_path(@menu_list) %></button>
